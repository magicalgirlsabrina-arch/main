apiVersion: 1

providers:
  - name: openclaw
    orgId: 1
    folder: OpenClaw
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
